#include "../../include/op.h"
#include <stdio.h>

int inst_st(champion_t *champion, core_t *core, code_t code, int *inst) {
  printf("called st\n");
  UNUSED(champion);
  UNUSED(core);
  UNUSED(code);
  UNUSED(inst);
  return 0;
};
