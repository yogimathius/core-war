#include "../../include/op.h"
#include <stdio.h>

int inst_sub(champion_t *champion, core_t *core, code_t code, int *inst) {
  printf("calling sub\n");
  UNUSED(champion);
  UNUSED(core);
  UNUSED(code);
  UNUSED(inst);
  return 0;
};
