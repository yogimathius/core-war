#include "../../include/op.h"
#include <stdio.h>

int inst_ldi(champion_t *champion, core_t *core, code_t code, int *inst) {
  printf("called ldi\n");
  UNUSED(champion);
  UNUSED(core);
  UNUSED(code);
  UNUSED(inst);
  return 0;
};
